// Event:-
    //new request
    // A signal that something has happened

    // class contain property and function called method

const EventEmitter=require('events')
const emiiter=new EventEmitter();



const log=require('./3logger')
log('message')
//emit=>Making a noise,produce - signalling